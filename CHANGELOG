HISTORY

(git master)

  * [Enter new changes just after this line - do not remove this line]

(2.2.3)
  Add TCP ModBus TCP server on port 502

(2.2.2)
  Add rounding in web
  Add memory management wrapper
  Change stock heap to 0 byte

(2.2.2)
  Fix finish beep in charger
  Remove 'Unprotected' mode in Base
  Add disablecause in web
  Move MCU flags to toolchain.cmake

(2.2.1)
  Fix encoder increment on startup
  Add print regulator version in start window
  Fix charger

(2.2.0)
  Migrate to CMake
  Used modbus for communicate with regulator
  
(STABLE-2.1.5)
  ++ New features:
   2018-05-27:
   * plog.c, plog.h: new log utils from esp32
   * menuSystem.c : fix bug - read from null parameter descriptor
    
(STABLE-2.1.4)
  ++ New features:
  
  2018-05-04:
  * pvd.c: added check callback function pointer
  * drivers.c: enable pvd

(STABLE-2.1.3)
  ++ Bugfixes:
  
  2018-04-28:
  * gpio.c: remap gpio
  * stm32f4x7_eth_bsp.c: added reset PHY
  * startupTSK.c: fix bug with show default settings load message
  
  ++ New features:
  
  2018-04-28:
  * sysTimeMeas.c: incresized converter resolution to 64 bit
  * monitorTSK.c: change  the report format to microseconds output

  2018-04-27:
  * debugCore.c: added print SHCSR
  
(STABLE-2.1.2)
  ++ Bugfixes:
  
  2018-04-26:
  * sntp.c: fix bug: dous not set time if SNTP_DEBUG_LEVEL < 2
  
  ++ New features:
  
  2018-04-26:
  * lwipopts.h: refactor
  * startup_stm32f407xx.c: refactor

(STABLE-2.1.1)
  ++ Bugfixes:

  2018-04-25:
  * pingService.c: fix bug: raw pcb recv callback altered pbuf payload pointer without eating packet
                            pbuf_free: p->ref > 0

  ++ New features:
  
  * cc.h: applied a new generation of random numbers
  
  2018-04-24:
  * rand.c, rand.h: added hardware random number generator driver
